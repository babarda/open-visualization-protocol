<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-RNK-03 LOLLIPOP rendered in language DL-12 NOROSHI -->

## LOLLIPOP chart, NOROSHI style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-RNK-03",
  "ovp_version": "1.0",
  "intent": [
    "ranking",
    "comparison"
  ],
  "form": "stick + dot, sorted descending",
  "data_required": "one categorical dimension + one non-negative measure; lighter ink than bars",
  "family": "RNK",
  "alternatives": [
    "CH-RNK-01 (denser data)"
  ],
  "see_instead": [
    {
      "when": "many close values",
      "use": "CH-RNK-01"
    }
  ]
}
```

Use when:
- ranking where bars would overwhelm the page (many rows, light values)
- editorial pages that want less ink

Do NOT use when:
- values very close together (dots overlap visually less than bars separate)
- natural order (CH-MAG-01)

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "on first value label",
  "categories": "array of {label, value >= 0, highlight?}"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 208,
      "right": 820,
      "top": 140
    },
    "row_height": 28,
    "row_gap": 22,
    "category_label": {
      "size": 13,
      "offset": 16
    },
    "value_label": {
      "size": 12,
      "offset": 12
    }
  },
  "scale": {
    "note": "sticks scale to max value; direct labels"
  }
}
```

### Rules
- sorted descending
- stick from zero to value, dot at value
- one highlight maximum
- unit once

### Never do this
- dots without sticks (loses the zero anchor)
- truncated baseline

### QA before delivering
- [ ] descending order
- [ ] sticks start at zero
- [ ] highlight <= 1
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| stick | `#E2E8F0` |
| dot | `#94A3B8` |
| dot_highlight | `#D63A2F` |
| category_label | `#475569` |
| value_label | `#0F172A` |
| baseline | `#E2E8F0` |
| title | `#0F172A` |
| source | `#475569` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 700
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
