<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-RNK-06 TABLE WITH BARS rendered in language DL-04 SHILPA -->

## TABLE WITH BARS chart, SHILPA style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-RNK-06",
  "ovp_version": "1.0",
  "intent": [
    "ranking",
    "lookup"
  ],
  "form": "table rows with an inline magnitude bar and a signed delta column",
  "data_required": "categories with one exact value each, plus a change vs a prior period",
  "family": "RNK",
  "alternatives": [
    "CH-RNK-01 (bar alone when exact lookup is secondary)",
    "CH-TAB-03 (KPI table, planned)"
  ],
  "see_instead": [
    {
      "when": "no delta column is needed",
      "use": "CH-RNK-01"
    }
  ]
}
```

Use when:
- readers need exact values AND instant rank perception
- weekly lists where the change column carries the action

Do NOT use when:
- more than 10 rows (paginate or aggregate)
- no exact-value need (CH-RNK-01)

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "on first value label",
  "value_header": "CAPS column header over the bars",
  "delta_header": "CAPS column header over the delta column",
  "categories": "array of {label, value, delta, highlight?}"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 250,
      "right": 640,
      "top": 150
    },
    "row_height": 34,
    "label_gutter": 218,
    "delta_x": 820,
    "header_label": {
      "size": 11,
      "offset": 14
    },
    "category_label": {
      "size": 13,
      "offset": 16
    },
    "value_label": {
      "size": 12,
      "offset": 10
    }
  }
}
```

### Rules
- rows sorted descending by value
- deltas signed and colored by sign, zero stays neutral ink
- hairline rules between rows, no zebra fills
- one highlight maximum

### Never do this
- zebra striping
- unsorted rows
- sparklines squeezed into cells
- coloring the whole row by status

### QA before delivering
- [ ] rows sorted descending
- [ ] deltas signed
- [ ] unit once
- [ ] highlight <= 1
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FBFDFF` |
| header | `#33424E` |
| rule | `#C6D5E0` |
| category_label | `#33424E` |
| bar | `#C6D5E0` |
| bar_highlight | `#1B6E8F` |
| value_label | `#143D59` |
| delta_pos | `#2E6E4E` |
| delta_neg | `#9E2B25` |
| title | `#143D59` |
| source | `#33424E` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 600
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
