<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-TIM-09 GANTT rendered in language DL-11 SUTRA -->

## GANTT chart, SUTRA style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-TIM-09",
  "ovp_version": "1.0",
  "intent": [
    "schedule",
    "progress"
  ],
  "form": "task bars on a period grid with done-overlay, milestones, today line",
  "data_required": "tasks with start/end in axis units + optional done_to and milestones + a today position",
  "family": "TIM",
  "alternatives": [
    "CH-TIM-15 (moments, not durations)",
    "CH-TIM-02 (overall progress vs plan)"
  ],
  "see_instead": [
    {
      "when": "only dated moments matter",
      "use": "CH-TIM-15"
    }
  ]
}
```

Use when:
- schedule communication: 4-10 tasks against a period grid
- showing done vs remaining per task plus the critical milestone

Do NOT use when:
- full CPM networks with dependencies (planning software, not a slide)
- more than ~10 rows (summarize to work packages)

### Input data shape
```json
{
  "title": "action title naming the driving task or date",
  "eyebrow": "caps, DL chrome only",
  "source": "bottom-left always",
  "axis": "{labels: [period names]}; task units are fractional axis positions",
  "tasks": "array of {label, start?, end?, done_to?, milestone_at?}",
  "today": "axis position of the status date"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 208,
      "right": 896,
      "top": 152
    },
    "row_height": 26,
    "row_gap": 18,
    "header_label": {
      "size": 10,
      "offset": 12
    },
    "category_label": {
      "size": 12,
      "offset": 16
    },
    "today_label": {
      "size": 9
    }
  },
  "scale": {
    "note": "x maps axis units 0..n linearly; period gridlines at every boundary"
  }
}
```

### Rules
- bars muted, done-portion overlaid in primary
- milestones are diamonds in ink; the today line is negative, dashed, labeled
- tasks in schedule order, never re-sorted
- 10 tasks maximum

### Never do this
- rainbow bars per task
- fake dependency arrows crisscrossing
- percent labels on every bar (the overlay shows it)
- 3D bars

### QA before delivering
- [ ] start < end and done_to within [start, end] on every task
- [ ] today line present and labeled
- [ ] tasks <= 10
- [ ] title names the driver with a date
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| grid | `#C9D6EE` |
| header | `#262626` |
| category_label | `#262626` |
| bar | `#A9BBD9` |
| done | `#1F3864` |
| milestone | `#1F3864` |
| today | `#9E2B25` |
| title | `#1F3864` |
| source | `#262626` |

### Fonts

- Title font: Cambria,Georgia,serif, weight 700
- All other text: Cambria,Georgia,serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
