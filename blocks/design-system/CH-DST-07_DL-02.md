<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-DST-07 BEESWARM rendered in language DL-02 KATA -->

## BEESWARM chart, KATA style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-DST-07",
  "ovp_version": "1.0",
  "intent": [
    "distribution",
    "outlier-detection"
  ],
  "form": "one dot per item packed without overlap along a value axis",
  "data_required": "one pooled group of up to ~60 items, each individually meaningful",
  "family": "DST",
  "alternatives": [
    "CH-DST-03 (values split by category)",
    "CH-DST-01 (larger samples)"
  ],
  "see_instead": [
    {
      "when": "values split across categories",
      "use": "CH-DST-03"
    },
    {
      "when": "too many items to name",
      "use": "CH-DST-01"
    }
  ]
}
```

Use when:
- every item matters and outliers deserve a name
- one pooled distribution where density and gaps are the story

Do NOT use when:
- more than ~60 items (CH-DST-01)
- several categories compared (CH-DST-03)

### Input data shape
```json
{
  "title": "action title naming the outlier",
  "source": "bottom-left always",
  "unit": "on the top axis tick and the highlight label",
  "points": "array of {label, value, highlight?}"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 120,
      "right": 820
    },
    "center_y": 300,
    "axis_y": 430,
    "dot_r": 6,
    "point_label": {
      "size": 12,
      "offset": 8
    },
    "tick_label": {
      "size": 11,
      "offset": 18
    }
  },
  "scale": {
    "x_zero": true,
    "target_ticks": 5,
    "nice_steps": [
      1,
      2,
      5,
      10
    ],
    "algorithm": "FN-05 nice scale"
  }
}
```

### Rules
- deterministic packing: values sorted, first free lane out from the
- center; no random jitter, ever
- the highlight dot is labeled with name and value; nothing else is
- zero-based value axis below the swarm

### Never do this
- random jitter
- labeling every dot
- sized dots (use CH-COR-02)
- more than ~60 points

### QA before delivering
- [ ] packing deterministic
- [ ] highlight <= 1 and labeled
- [ ] zero-based axis
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| dot | `#B7DCDC` |
| dot_highlight | `#008080` |
| label | `#222222` |
| baseline | `#222222` |
| tick_label | `#555555` |
| title | `#222222` |
| source | `#555555` |

### Fonts

- Title font: 'Rethink Sans','Segoe UI',sans-serif, weight 600
- All other text: 'Poppins','Segoe UI',sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
