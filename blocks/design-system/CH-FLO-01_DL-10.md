<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-FLO-01 WATERFALL rendered in language DL-10 BASIRA -->

## WATERFALL chart, BASIRA style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-FLO-01",
  "ovp_version": "1.0",
  "intent": [
    "flow",
    "bridge",
    "variance-decomposition"
  ],
  "form": "waterfall / bridge",
  "data_required": "a start total + signed delta steps; end total is computed, never supplied",
  "family": "FLO",
  "alternatives": [
    "CH-DEV-01 (diverging bar, when steps are independent)"
  ],
  "see_instead": [
    {
      "when": "deltas are independent, not sequential",
      "use": "CH-DEV-01"
    },
    {
      "when": "composition of a static total",
      "use": "CH-PTW-01"
    }
  ]
}
```

Use when:
- explaining how a total moved from A to B through named drivers
- cost bridges, budget-to-forecast, headcount walks
- 3 to 7 delta steps

Do NOT use when:
- independent variances with no running total (use CH-DEV-01)
- more than 7 steps (aggregate the tail)
- any cumulative level would go below zero on the canvas

### Input data shape
```json
{
  "title": "string, action title stating the end-to-end move with a number",
  "eyebrow": "string caps, rendered only when the DL chrome asks for it",
  "source": "string, rendered bottom-left, always",
  "unit": "string, appears once, on the start bar label",
  "start": "{label, value}",
  "steps": "array of {label, delta} with sign; rendered in given order",
  "end_label": "string label for the computed end bar"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 64,
      "right": 896,
      "top": 144,
      "bottom": 432
    },
    "bar_width": 56,
    "value_label": {
      "size": 13,
      "offset": 8
    },
    "x_label": {
      "size": 11,
      "offset": 20
    }
  },
  "scale": {
    "note": "bars scale to the max cumulative level; baseline zero; direct labels, no axis"
  }
}
```

### Rules
- the end bar is COMPUTED from start + deltas; supplying it is a validation error
- increases and decreases colored by semantic roles, totals in ink
- delta labels signed (+12 / -6); unit once on the start label
- dashed connectors carry the running level between bars
- step order is the data's order; the renderer never re-sorts a bridge

### Never do this
- supplying the end total as data (it must reconcile by construction)
- unsigned delta labels
- sorting steps by size (a bridge is a narrative, keep the told order)
- floating bars without connectors
- 3D depth

### QA before delivering
- [ ] end bar equals start + sum of deltas
- [ ] every delta label carries its sign
- [ ] unit exactly once, on the start label
- [ ] connectors present between all bars
- [ ] title states the end-to-end move with a number
- [ ] source line present bottom-left

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#0B0B0B` |
| total | `#FFFFFF` |
| increase | `#E5534B` |
| decrease | `#3FB68B` |
| connector | `#3A3F44` |
| value_label | `#FFFFFF` |
| x_label | `#D9D9D9` |
| baseline | `#3A3F44` |
| title | `#FFFFFF` |
| source | `#D9D9D9` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 300
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
