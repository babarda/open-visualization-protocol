<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-TIM-07 CALENDAR HEATMAP rendered in language DL-13 MIZAN -->

## CALENDAR HEATMAP chart, MIZAN style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-TIM-07",
  "ovp_version": "1.0",
  "intent": [
    "pattern",
    "change-over-time",
    "density"
  ],
  "form": "weekday x week grid, shaded and numbered",
  "data_required": "a count or measure per weekday per week; the weekly rhythm is the message",
  "family": "TIM",
  "alternatives": [
    "CH-COR-03 (generic two-dimension grids)"
  ],
  "see_instead": [
    {
      "when": "dimensions are not calendar units",
      "use": "CH-COR-03"
    }
  ]
}
```

Use when:
- workload rhythm: deliveries, tickets, tests per weekday across weeks
- capacity planning around recurring peaks

Do NOT use when:
- non-calendar dimensions (CH-COR-03)
- single week (a column chart does it)

### Input data shape
```json
{
  "title": "action title naming the rhythm",
  "source": "bottom-left always",
  "columns": "week labels",
  "rows": "weekday rows {label, values[]}"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 140,
      "right": 896,
      "top": 168
    },
    "row_height": 56,
    "header_label": {
      "size": 10,
      "offset": 14
    },
    "row_label": {
      "size": 12,
      "offset": 16
    },
    "value_label": {
      "size": 11
    }
  },
  "scale": {
    "normalize": "global",
    "note": "one scale across the whole calendar; weekday order fixed Mon-first"
  }
}
```

### Rules
- weekday rows in calendar order, always
- values printed in every cell
- single-hue ramp; global normalization

### Never do this
- sorting weekdays by value
- rainbow ramps
- cells without values

### QA before delivering
- [ ] weekday order fixed
- [ ] every cell numbered
- [ ] title names the rhythm
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#F7EFE3` |
| cell_low | `#F1E5D3` |
| cell_high | `#C4572E` |
| header | `#4E4237` |
| row_label | `#31261D` |
| value | `#4E4237` |
| title | `#31261D` |
| source | `#4E4237` |

### Fonts

- Title font: 'EB Garamond',Georgia,serif, weight 600
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
