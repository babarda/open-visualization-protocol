<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-RNK-02 ORDERED COLUMN rendered in language DL-13 MIZAN -->

## ORDERED COLUMN chart, MIZAN style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-RNK-02",
  "ovp_version": "1.0",
  "intent": [
    "ranking",
    "comparison"
  ],
  "form": "vertical column, sorted descending",
  "data_required": "one categorical dimension + one non-negative measure, short names",
  "family": "RNK",
  "alternatives": [
    "CH-RNK-01 (long names)"
  ],
  "see_instead": [
    {
      "when": "natural order matters",
      "use": "CH-MAG-01"
    }
  ]
}
```

Use when:
- ranking with short category names where vertical reads naturally
- 8 categories or fewer

Do NOT use when:
- long names (CH-RNK-01)
- natural order (CH-MAG-01)

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "on first value label",
  "categories": "array of {label, value >= 0, highlight?}"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 64,
      "right": 896,
      "top": 144,
      "bottom": 432
    },
    "bar_width": 64,
    "category_label": {
      "size": 12,
      "offset": 20
    },
    "value_label": {
      "size": 13,
      "offset": 8
    }
  },
  "scale": {
    "sort": "desc",
    "note": "renderer sorts descending; ties break alphabetically"
  }
}
```

### Rules
- sorted descending, always
- columns start at zero
- direct labels, no axis
- one highlight maximum
- unit once

### Never do this
- unsorted ranking
- truncated baseline
- rainbow columns

### QA before delivering
- [ ] descending order
- [ ] baseline at zero
- [ ] highlight <= 1
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#F7EFE3` |
| bar | `#C9BBA4` |
| bar_highlight | `#C4572E` |
| value_label | `#31261D` |
| category_label | `#4E4237` |
| baseline | `#DCCFB6` |
| title | `#31261D` |
| source | `#4E4237` |

### Fonts

- Title font: 'EB Garamond',Georgia,serif, weight 600
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
