<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-DST-04 BOXPLOT rendered in language DL-09 ATLAS -->

## BOXPLOT chart, ATLAS style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-DST-04",
  "ovp_version": "1.0",
  "intent": [
    "distribution",
    "comparison"
  ],
  "form": "five-number summary per category: whiskers, quartile box, median tick",
  "data_required": "categories with a declared five-number summary (min, q1, median, q3, max)",
  "family": "DST",
  "alternatives": [
    "CH-DST-03 (few raw values)",
    "CH-DST-08 (shape beyond quartiles, warn)"
  ],
  "see_instead": [
    {
      "when": "few enough values to show raw",
      "use": "CH-DST-03"
    },
    {
      "when": "the audience reads quartiles poorly",
      "use": "CH-DST-03"
    }
  ]
}
```

Use when:
- comparing many distributions compactly
- audiences trained on quartiles (analysts, engineers)

Do NOT use when:
- general audiences (boxplots are widely misread; CH-DST-03)
- multimodal shapes (a box hides two humps; CH-DST-08 or CH-DST-09)

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "on first median label",
  "categories": "array of {label, min, q1, med, q3, max, highlight?}",
  "note": "summaries are DECLARED inputs; OVP renders, it does not compute"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 208,
      "right": 790,
      "top": 160
    },
    "row_height": 36,
    "row_gap": 20,
    "tick_label": {
      "size": 11,
      "offset": 18
    },
    "category_label": {
      "size": 13,
      "offset": 16
    },
    "value_label": {
      "size": 11,
      "offset": 12
    }
  },
  "scale": {
    "x_zero": true,
    "target_ticks": 5,
    "nice_steps": [
      1,
      2,
      5,
      10
    ],
    "algorithm": "FN-05 nice scale"
  }
}
```

### Rules
- five-number summaries are supplied, never computed silently
- median printed right of the max whisker, labeled on the first row
- whisker convention (min/max, not 1.5 IQR) must hold for every row
- shared zero-based scale

### Never do this
- mixing whisker conventions across rows
- boxplots for general audiences without a reading key
- hiding sample sizes

### QA before delivering
- [ ] min <= q1 <= median <= q3 <= max per row
- [ ] median labeled once
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#F4EFE6` |
| box | `#B9AE9C` |
| median | `#0F766E` |
| whisker | `#2B3A42` |
| baseline | `#2B3A42` |
| tick_label | `#4A5860` |
| category_label | `#4A5860` |
| value_label | `#2B3A42` |
| title | `#2B3A42` |
| source | `#4A5860` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 600
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
