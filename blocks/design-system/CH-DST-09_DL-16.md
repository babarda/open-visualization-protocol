<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-DST-09 DENSITY CURVE rendered in language DL-16 TELOS -->

## DENSITY CURVE chart, TELOS style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-DST-09",
  "ovp_version": "1.0",
  "intent": [
    "distribution",
    "density"
  ],
  "form": "one smooth declared density curve, filled to zero",
  "data_required": "a DECLARED density curve: grid points + density shares, bandwidth stated",
  "family": "DST",
  "alternatives": [
    "CH-DST-01 (honest bins, no smoothing)",
    "CH-DST-08 (mirrored, per category)"
  ],
  "see_instead": [
    {
      "when": "bins are honest enough",
      "use": "CH-DST-01"
    },
    {
      "when": "comparing shapes across categories",
      "use": "CH-DST-08"
    }
  ]
}
```

Use when:
- one distribution's shape for a reader who knows what a density is
- the curve is already computed and declared, with its bandwidth

Do NOT use when:
- audiences who read areas as counts (CH-DST-01)
- several overlapping curves (small multiples, not overlays)

### Input data shape
```json
{
  "title": "action title naming the peak",
  "source": "bottom-left always, must state the density bandwidth",
  "unit": "%, on the top tick",
  "x": "ascending grid points as labels",
  "series": "exactly one role 'key': the density shares"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 96,
      "right": 756,
      "top": 128,
      "bottom": 452
    },
    "tick_label": {
      "size": 11,
      "offset": 12
    },
    "x_label": {
      "size": 11,
      "offset": 20
    },
    "series_label": {
      "size": 13,
      "offset": 10
    },
    "stroke": {
      "key": 2.5,
      "context": 2
    },
    "fill_opacity": 0.15,
    "annotation": {
      "size": 12,
      "style": "italic"
    }
  },
  "scale": {
    "y_zero": true,
    "target_ticks": 5,
    "nice_steps": [
      1,
      2,
      5,
      10
    ],
    "algorithm": "FN-05 nice scale"
  }
}
```

### Rules
- the density is supplied, never computed silently; the source line
- states the bandwidth
- one curve only; comparisons go to CH-DST-08 or small multiples
- y starts at zero

### Never do this
- overlapping filled densities
- silent KDE with hidden bandwidth
- reading the y axis as counts

### QA before delivering
- [ ] bandwidth stated in the source
- [ ] one filled curve
- [ ] y starts at zero
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| key | `#C4001A` |
| context | `#8C8880` |
| gridline | `#E8E6E2` |
| zero_line | `#D9D6D2` |
| tick_label | `#444444` |
| x_label | `#444444` |
| key_label | `#C4001A` |
| context_label | `#444444` |
| title | `#1A1A1A` |
| source | `#444444` |
| annotation | `#444444` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 700
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
