<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-PTW-10 CIRCLE PACK rendered in language DL-06 LEX -->

## CIRCLE PACK chart, LEX style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-PTW-10",
  "ovp_version": "1.0",
  "intent": [
    "part-to-whole",
    "magnitude"
  ],
  "form": "nested-look circle cluster, AREA proportional to value",
  "data_required": "3-12 positive values where an organic cluster beats a grid",
  "family": "PTW",
  "alternatives": [
    "CH-PTW-05 (same data, readable rectangles)",
    "CH-RNK-01 (exact ranking)"
  ],
  "see_instead": [
    {
      "when": "areas must be compared",
      "use": "CH-PTW-05"
    },
    {
      "when": "rank matters",
      "use": "CH-RNK-01"
    }
  ]
}
```

Use when:
- WARN: editorial openers where an organic cluster invites exploration
- one-off overviews where precision is explicitly not the point

Do NOT use when:
- comparing circle areas (worse than rectangles, much worse than bars)
- any recurring report (the pack layout shifts as values change)

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "on the largest circle",
  "categories": "[{label, value, highlight?}]"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 240,
      "right": 720,
      "top": 130,
      "bottom": 470
    },
    "r_max": 110,
    "name_label": {
      "size": 12
    }
  }
}
```

### Rules
- deterministic spiral packing, published algorithm; no random layouts
- labels inside circles that fit, above the rim otherwise
- one highlight maximum

### Never do this
- WARN ENTRY: circle areas are the least readable encoding that still counts as a chart; CH-PTW-05 whenever anyone must compare
- random or physics layouts
- nesting deeper than one level

### QA before delivering
- [ ] deterministic layout
- [ ] values printed where circles fit
- [ ] highlight <= 1
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| cell | `#B9B9B9` |
| cell_highlight | `#7A1F1F` |
| label | `#333333` |
| title | `#1A1A1A` |
| source | `#333333` |

### Fonts

- Title font: Georgia,serif, weight 700
- All other text: Georgia,serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
