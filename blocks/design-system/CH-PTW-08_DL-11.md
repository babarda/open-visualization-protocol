<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-PTW-08 FUNNEL rendered in language DL-11 SUTRA -->

## FUNNEL chart, SUTRA style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-PTW-08",
  "ovp_version": "1.0",
  "intent": [
    "flow",
    "share"
  ],
  "form": "centered decreasing bars with conversion rates",
  "data_required": "ordered stages where each is a subset of the previous; attrition is the message",
  "family": "PTW",
  "alternatives": [
    "CH-FLO-01 (gains and losses, not pure attrition)"
  ],
  "see_instead": [
    {
      "when": "steps can gain as well as lose",
      "use": "CH-FLO-01"
    }
  ]
}
```

Use when:
- pipeline attrition: ordered > shipped > cleared > delivered
- conversion reporting with the rate at each step

Do NOT use when:
- non-subset stages (waterfall)
- more than 6 stages (aggregate)

### Input data shape
```json
{
  "title": "action title naming the widest drop",
  "source": "bottom-left always",
  "unit": "on first stage label",
  "stages": "array of {label, value}, non-increasing"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 240,
      "right": 720,
      "top": 140
    },
    "row_height": 44,
    "row_gap": 24,
    "value_label": {
      "size": 12
    }
  },
  "scale": {
    "note": "widths scale to the first stage; conversion % printed between stages"
  }
}
```

### Rules
- stages must be non-increasing (validated)
- conversion percentage printed between every pair of stages
- first stage takes primary; the rest recede to muted

### Never do this
- funnel shapes with slanted sides (fake geometry)
- increasing stages
- more than 6 stages

### QA before delivering
- [ ] stages non-increasing
- [ ] conversion rates printed
- [ ] stages <= 6
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| stage | `#A9BBD9` |
| stage_first | `#1F3864` |
| stage_label | `#262626` |
| value_label | `#1F3864` |
| conversion | `#262626` |
| title | `#1F3864` |
| source | `#262626` |

### Fonts

- Title font: Cambria,Georgia,serif, weight 700
- All other text: Cambria,Georgia,serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
