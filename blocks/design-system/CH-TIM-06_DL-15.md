<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-TIM-06 SPARKLINE STRIP rendered in language DL-15 ABRID -->

## SPARKLINE STRIP chart, ABRID style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-TIM-06",
  "ovp_version": "1.0",
  "intent": [
    "trend",
    "status"
  ],
  "form": "one micro-trend per row: shape, last value, signed change",
  "data_required": "3-8 metrics, each with 5+ ordered values and a declared good direction",
  "family": "TIM",
  "alternatives": [
    "CH-TAB-03 (exact values + deltas)",
    "CH-TIM-01 (one metric, full size)"
  ],
  "see_instead": [
    {
      "when": "exact values must be read",
      "use": "CH-TAB-03"
    },
    {
      "when": "one metric deserves a full chart",
      "use": "CH-TIM-01"
    }
  ]
}
```

Use when:
- scanning many metrics for direction at a glance
- weekly one-pagers where shape matters, not exact history

Do NOT use when:
- comparing magnitudes across rows (each row has its own scale)
- fewer than 5 points per row

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "on first row's last value",
  "rows": "[{label, values, good: up|down}]"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 208,
      "right": 800,
      "top": 160
    },
    "row_height": 40,
    "row_gap": 18,
    "spark_left": 240,
    "spark_right": 560,
    "spark_half": 11,
    "delta_x": 800,
    "category_label": {
      "size": 13,
      "offset": 16
    },
    "value_label": {
      "size": 12,
      "offset": 12
    }
  }
}
```

### Rules
- each row scales to its own min-max: sparklines show shape, never comparable magnitude
- delta colored by the row's DECLARED good direction, not by sign
- end dot marks the latest value

### Never do this
- shared scale implied across rows
- axes or gridlines on sparklines
- coloring deltas by sign when down is good

### QA before delivering
- [ ] good direction declared per row
- [ ] >= 5 points per row
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| line | `#0E3A2C` |
| dot | `#197A56` |
| value_label | `#0E3A2C` |
| delta_pos | `#197A56` |
| delta_neg | `#9E2B25` |
| category_label | `#3E4C46` |
| title | `#0E3A2C` |
| source | `#3E4C46` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 700
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
