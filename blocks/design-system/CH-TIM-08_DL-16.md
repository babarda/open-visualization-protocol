<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-TIM-08 FAN rendered in language DL-16 TELOS -->

## FAN chart, TELOS style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-TIM-08",
  "ovp_version": "1.0",
  "intent": [
    "trend",
    "uncertainty"
  ],
  "form": "actual line continuing into declared forecast bands",
  "data_required": "actuals + forecast median with declared p25-p75 and p10-p90 bands",
  "family": "TIM",
  "alternatives": [
    "CH-TIM-02 (progress vs plan, no uncertainty)",
    "CH-DST-10 (uncertainty on categories)"
  ],
  "see_instead": [
    {
      "when": "no uncertainty statement exists",
      "use": "CH-TIM-01"
    },
    {
      "when": "commitment vs plan is the story",
      "use": "CH-TIM-02"
    }
  ]
}
```

Use when:
- forecasts that honestly show their spread
- committing to a range instead of a false point estimate

Do NOT use when:
- fabricating bands the model never produced
- showing only the median (that is CH-TIM-01 with a dashed tail)

### Input data shape
```json
{
  "title": "action title naming the range",
  "source": "must state the forecast method",
  "unit": "on the top tick",
  "x": "all period labels, actual + forecast",
  "split": "count of actual periods",
  "actual": "values for the first split periods",
  "median": "forecast medians",
  "band50": "[[lo,hi]] p25-p75",
  "band90": "[[lo,hi]] p10-p90"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 96,
      "right": 756,
      "top": 128,
      "bottom": 452
    },
    "tick_label": {
      "size": 11,
      "offset": 12
    },
    "x_label": {
      "size": 11,
      "offset": 20
    },
    "series_label": {
      "size": 12,
      "offset": 10
    },
    "fill_opacity": {
      "outer": 0.15,
      "inner": 0.3
    }
  },
  "scale": {
    "y_zero": true,
    "target_ticks": 5,
    "nice_steps": [
      1,
      2,
      5,
      10
    ],
    "algorithm": "FN-05 nice scale"
  }
}
```

### Rules
- bands are DECLARED quantiles; the source names the method
- median dashed: it is an estimate, not a measurement
- the forecast region is visibly divided from actuals
- y starts at zero

### Never do this
- bands without a named method
- solid median implying certainty
- hiding the actual/forecast boundary

### QA before delivering
- [ ] method in the source line
- [ ] band90 contains band50 contains median
- [ ] median dashed
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| actual | `#1A1A1A` |
| median | `#C4001A` |
| band | `#C4001A` |
| divider | `#D9D6D2` |
| label | `#444444` |
| gridline | `#E8E6E2` |
| zero_line | `#D9D6D2` |
| tick_label | `#444444` |
| x_label | `#444444` |
| title | `#1A1A1A` |
| source | `#444444` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 700
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
