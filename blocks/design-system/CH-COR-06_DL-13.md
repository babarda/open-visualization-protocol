<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-COR-06 PARALLEL COORDINATES rendered in language DL-13 MIZAN -->

## PARALLEL COORDINATES chart, MIZAN style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-COR-06",
  "ovp_version": "1.0",
  "intent": [
    "correlation",
    "pattern"
  ],
  "form": "one polyline per item across 3-5 vertical zero-based axes",
  "data_required": "6-12 items measured on 3-5 dimensions; one item highlighted",
  "family": "COR",
  "alternatives": [
    "CH-COR-01 (two dimensions)",
    "CH-TAB-02 (exact values per cell)"
  ],
  "see_instead": [
    {
      "when": "two dimensions suffice",
      "use": "CH-COR-01"
    },
    {
      "when": "exact per-cell reading",
      "use": "CH-TAB-02"
    }
  ]
}
```

Use when:
- WARN: analyst-only views for spotting multi-dimensional profiles
- finding which items move together across 3-5 measures

Do NOT use when:
- general audiences (line crossings read as meaning; they are layout)
- more than ~12 items (spaghetti)

### Input data shape
```json
{
  "title": "action title naming the profile",
  "source": "bottom-left always",
  "axes": "[{name, unit}] 3-5, each zero-based",
  "items": "[{label, values, highlight?}]"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 150,
      "right": 780,
      "top": 170,
      "bottom": 450
    },
    "axis_label": {
      "size": 12,
      "offset": 28
    },
    "line_label": {
      "size": 12,
      "offset": 12
    }
  },
  "scale": {
    "target_ticks": 5,
    "nice_steps": [
      1,
      2,
      5,
      10
    ],
    "algorithm": "FN-05 nice scale"
  }
}
```

### Rules
- every axis zero-based with its own top, both printed at the axis head
- context lines muted and translucent; the one highlight carries the story
- axis order is part of the spec: reordering changes the picture

### Never do this
- WARN ENTRY: crossings between axes are artifacts of axis order, not relationships; analysts only
- min-max normalization hiding the zero
- more than 12 lines

### QA before delivering
- [ ] axes zero-based and labeled
- [ ] highlight <= 1
- [ ] <= 12 items
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#F7EFE3` |
| axis | `#DCCFB6` |
| axis_label | `#4E4237` |
| tick_label | `#4E4237` |
| line | `#C9BBA4` |
| line_highlight | `#C4572E` |
| title | `#31261D` |
| source | `#4E4237` |

### Fonts

- Title font: 'EB Garamond',Georgia,serif, weight 600
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
