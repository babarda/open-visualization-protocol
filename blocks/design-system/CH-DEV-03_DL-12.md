<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-DEV-03 SPINE rendered in language DL-12 NOROSHI -->

## SPINE chart, NOROSHI style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-DEV-03",
  "ovp_version": "1.0",
  "intent": [
    "part-to-whole",
    "comparison"
  ],
  "form": "one bar per category split into two parts around a center axis",
  "data_required": "categories with two complementary parts (done/remaining, yes/no)",
  "family": "DEV",
  "alternatives": [
    "CH-PTW-02 (100% stacked for 3-4 parts)",
    "CH-DEV-05 (agreement scales with 4 levels)"
  ],
  "see_instead": [
    {
      "when": "3 or more parts per category",
      "use": "CH-PTW-02"
    },
    {
      "when": "graded agreement levels",
      "use": "CH-DEV-05"
    }
  ]
}
```

Use when:
- two-part splits compared across categories
- reading which side of a balance each category falls on

Do NOT use when:
- more than two parts (CH-PTW-02)
- parts that do not sum to a meaningful whole

### Input data shape
```json
{
  "title": "action title with a number",
  "source": "bottom-left always",
  "unit": "appended to first-row values (%, usually)",
  "series_labels": "[left_name, right_name], printed above the first row",
  "categories": "array of {label, a, b}; a+b normalized per row"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "plot": {
      "left": 208,
      "right": 820,
      "top": 170
    },
    "row_height": 26,
    "row_gap": 16,
    "bar_height": 20,
    "series_label": {
      "size": 10,
      "offset": 8,
      "gap": 10
    },
    "category_label": {
      "size": 13,
      "offset": 16
    },
    "value_label": {
      "size": 12,
      "offset": 8
    }
  }
}
```

### Rules
- the split point sits on the center axis; bar width is constant
- left part muted, right part primary
- values printed at both outer ends
- sides named above the first row, never a legend

### Never do this
- three or more segments
- legends
- rows normalized to different totals without saying so

### QA before delivering
- [ ] two parts per row
- [ ] center axis drawn
- [ ] sides named once
- [ ] unit once
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| side_a | `#94A3B8` |
| side_b | `#D63A2F` |
| axis | `#0F172A` |
| category_label | `#475569` |
| value_label | `#0F172A` |
| title | `#0F172A` |
| source | `#475569` |

### Fonts

- Title font: 'Segoe UI',Arial,sans-serif, weight 700
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
