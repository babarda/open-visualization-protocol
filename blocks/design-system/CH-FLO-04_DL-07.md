<!-- Generated by VizCatalogue. Do not hand-edit; regenerate from source. -->
<!-- Chart CH-FLO-04 CHORD rendered in language DL-07 SAGA -->

## CHORD chart, SAGA style

Render as SVG (or equivalent) on a 960x540 px canvas.
Every position, size, and color below is exact. No adjectives, no improvisation.

### AI metadata (for agent routing)
```json
{
  "code": "CH-FLO-04",
  "ovp_version": "1.0",
  "intent": [
    "flow",
    "pattern"
  ],
  "form": "nodes on a circle, curved ribbons weighted by flow",
  "data_required": "2-6 nodes with a square flow matrix; totals matter more than pairs",
  "family": "FLO",
  "alternatives": [
    "CH-FLO-02 (readable flows, planned/built as sankey)",
    "CH-TAB-02 (exact matrix values)"
  ],
  "see_instead": [
    {
      "when": "flows must be read",
      "use": "CH-FLO-02"
    },
    {
      "when": "exact values per pair",
      "use": "CH-TAB-02"
    }
  ]
}
```

Use when:
- WARN: editorial overviews of dense mutual exchange between few nodes
- showing that everything trades with everything, not how much

Do NOT use when:
- reading any specific flow (ribbon widths at a glance mislead)
- sparse matrices (a sankey tells it straight)

### Input data shape
```json
{
  "title": "action title naming the dominant exchange",
  "source": "bottom-left always",
  "unit": "flow unit",
  "nodes": "2-6 names",
  "flows": "square matrix, diagonal zero"
}
```

### Exact layout (px on the canvas, follow literally)
```json
{
  "canvas": {
    "width": 960,
    "height": 540
  },
  "layout": {
    "title": {
      "x": 32,
      "y": 56,
      "y_with_eyebrow": 88,
      "size": 24
    },
    "kicker": {
      "x": 32,
      "y": 28,
      "w": 24,
      "h": 4
    },
    "eyebrow": {
      "x": 32,
      "y": 52,
      "size": 11,
      "letter_spacing": 2
    },
    "source": {
      "x": 32,
      "y": 516,
      "size": 11
    },
    "cx": 430,
    "cy": 320,
    "r": 190,
    "ribbon_max": 16,
    "label": {
      "size": 12,
      "offset": 14
    }
  }
}
```

### Rules
- node arcs sized by total in+out; labels carry the totals
- ribbons colored by source, monochrome ramp
- diagonal must be zero: self-flows are not flows

### Never do this
- WARN ENTRY: chord diagrams decorate; CH-TAB-02 or a sankey informs
- self-loops
- more than 6 nodes
- rainbow ribbons

### QA before delivering
- [ ] square matrix, zero diagonal
- [ ] <= 6 nodes
- [ ] totals printed
- [ ] source present

### Colors (resolved, use these exact values)

| Element | Color |
|---|---|
| background | `#FFFFFF` |
| label | `#2E2E2E` |
| title | `#111111` |
| source | `#2E2E2E` |

### Fonts

- Title font: Georgia,serif, weight 700
- All other text: 'Segoe UI',Arial,sans-serif, weight 400 (600 on highlighted items)

### Language chrome

No kicker, no eyebrow; title at the default y.
